en:
  error:
    no_project: Project with id %{id} does not exists
    no_column: Column with id %{id} does not exists
    no_project: Project have now column with id %{id}
    not_owner: I'll remember it
    id_required: You must send the ID of the record you want to update 
